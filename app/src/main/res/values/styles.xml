<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

    <style name="NumPadButtons" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="layout_columnWeight">1</item>
        <item name="layout_rowWeight">1</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/numpad_button</item>
    </style>

    <style name="NumPad">
        <item name="android:background">@color/bg_numpad</item>
    </style>

    <style name="Text">
        <item name="android:textAlignment">viewEnd</item>
        <item name="android:fontFamily">@font/allerta</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

    <style name="StoryField" parent="Text">
        <item name="android:textColor">@color/story_text</item>
        <item name="android:textAlignment">textStart</item>
        <item name="autoSizeTextType">uniform</item>
    </style>

    <style name="InputField" parent="Text">
        <item name="android:background">@null</item>
        <!-- These two are responsible for one-line left scrolling of EditText -->
        <item name="android:maxLines">1</item>
        <item name="android:inputType">text</item>
    </style>

</resources>
